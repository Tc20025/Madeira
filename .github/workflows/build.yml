name: Build Madeira IPA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 360

    steps:
      - name: Checkout Madeira
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install build tools & Lock LLVM
        shell: bash
        run: |
          set -e
          brew install cmake ninja bison flex pkg-config sevenzip llvm@15
          
          # Force link LLVM 15 to suppress Homebrew warnings and guarantee toolchain priority
          brew link --force llvm@15
          
          echo 'export PATH="/opt/homebrew/opt/llvm@15/bin:/opt/homebrew/opt/bison/bin:/opt/homebrew/opt/flex/bin:/opt/homebrew/bin:$PATH"' >> $GITHUB_ENV
          echo 'export LDFLAGS="-L/opt/homebrew/opt/bison/lib -L/opt/homebrew/opt/flex/lib -L/opt/homebrew/opt/llvm@15/lib"' >> $GITHUB_ENV
          echo 'export CPPFLAGS="-I/opt/homebrew/opt/bison/include -I/opt/homebrew/opt/flex/include -I/opt/homebrew/opt/llvm@15/include -Wno-deprecated-declarations"' >> $GITHUB_ENV
          echo 'export CPLUS_INCLUDE_PATH="/opt/homebrew/opt/llvm@15/include"' >> $GITHUB_ENV
          echo 'export C_INCLUDE_PATH="/opt/homebrew/opt/llvm@15/include"' >> $GITHUB_ENV
          echo 'export LIBRARY_PATH="/opt/homebrew/opt/llvm@15/lib"' >> $GITHUB_ENV
          echo 'export LLVM_CONFIG="/opt/homebrew/opt/llvm@15/bin/llvm-config"' >> $GITHUB_ENV
          echo 'export CXXFLAGS="-Wno-deprecated-declarations -Wno-error"' >> $GITHUB_ENV

      - name: Install Wine manually
        shell: bash
        run: |
          set -e
          sudo /usr/sbin/softwareupdate --install-rosetta --agree-to-license || true

          curl -L --fail --retry 3 -O https://github.com/Gcenx/macOS_Wine_builds/releases/download/11.16/wine-devel-11.16-osx64.tar.xz
          tar -xf wine-devel-11.16-osx64.tar.xz
          xattr -rc "Wine Devel.app" || true
          
          WINE_BIN="$(pwd)/Wine Devel.app/Contents/Resources/wine/bin"
          mkdir -p /opt/homebrew/bin
          ln -sf "$WINE_BIN/wine" /opt/homebrew/bin/wine
          ln -sf "$WINE_BIN/wine64" /opt/homebrew/bin/wine64
          ln -sf "$WINE_BIN/wineboot" /opt/homebrew/bin/wineboot
          
          echo "WINE=/opt/homebrew/bin/wine" >> $GITHUB_ENV
          echo "WINEDLLOVERRIDES=mscoree,mshtml=" >> $GITHUB_ENV
          echo "WINEDEBUG=-all" >> $GITHUB_ENV

      - name: Fetch LLVM MinGW toolchain
        shell: bash
        run: |
          set -e
          mkdir -p toolchains
          cd toolchains
          curl -L --fail --retry 3 -O https://github.com/mstorsjo/llvm-mingw/releases/download/20260421/llvm-mingw-20260421-ucrt-macos-universal.tar.xz
          tar -xf llvm-mingw-20260421-ucrt-macos-universal.tar.xz
          rm llvm-mingw-20260421-ucrt-macos-universal.tar.xz

      - name: Fetch missing source dependencies (FreeType)
        shell: bash
        run: |
          set -e
          echo "=== Cloning FreeType ==="
          mkdir -p research
          git clone --depth 1 https://gitlab.freedesktop.org/freetype/freetype.git research/freetype

      - name: Generate Metal Shaders (Pre-Build)
        shell: bash
        run: |
          set -e
          echo "=== Pre-generating MSAD Shaders ==="
          mkdir -p build/dxmt-ios/shader-headers
          
          MSAD_METAL=$(find research/dxmt -name "msad.metal" | head -n 1)
          if [ -n "$MSAD_METAL" ]; then
            xcrun -sdk iphoneos metal -c "$MSAD_METAL" -o build/dxmt-ios/msad.air
            xcrun -sdk iphoneos metallib build/dxmt-ios/msad.air -o build/dxmt-ios/air_msad.metallib
            cd build/dxmt-ios
            xxd -i air_msad.metallib > shader-headers/air_msad.h
            cd ../..
            echo "Successfully generated air_msad.h"
          else
            echo "Warning: msad.metal not found. Assuming submodules are handling it."
          fi

      - name: Build Required Native Dependencies
        shell: bash
        run: |
          set -e
          wineboot --init || true
          
          REQUIRED_COMPONENTS=(
            "gmp"
            "nettle"
            "gnutls"
            "freetype-ios"
            "wine"
            "ntdll-unix"
            "win32u-unix"
            "wineserver"
            "fex"
            "dxmt"
            "dxmt-ios"
          )
          
          for component in "${REQUIRED_COMPONENTS[@]}"; do
            script="build/$component/build.sh"
            if [ -f "$script" ]; then
              echo ">>> Building $component <<<"
              if ! bash -x "$script"; then
                echo "::error::Build failed in $component"
                find "build/$component" -name "*.err" -type f -exec echo -e "\n>>> {} <<<" \; -exec cat {} \;
                exit 1
              fi
            else
              echo "Info: Script $script not found, proceeding..."
            fi
          done

      - name: Check required Madeira files
        shell: bash
        run: |
          MISSING_FILES=0
          for f in \
            app/libdxmt_unix.a \
            app/Madeira/libwineserver.a \
            app/Madeira/libntdll_unix.a \
            app/Madeira/libwin32u_unix.a \
            app/Madeira/libgnutls.a \
            app/Madeira/libhogweed.a \
            app/Madeira/libnettle.a \
            app/Madeira/libgmp.a \
            FEX/build-ios/FEXCore/Source/libFEXCore.a \
            FEX/build-ios/FEXCore/Source/libFEXCore_Base.a \
            FEX/build-ios/FEXCore/Source/libJemallocLibs.a
          do
            if [ ! -e "$f" ]; then
              echo "MISSING: $f"
              MISSING_FILES=1
            fi
          done
          
          if [ $MISSING_FILES -eq 1 ]; then
            exit 1
          fi

      - name: Prepare Microsoft VC++ Runtime
        shell: bash
        run: |
          set -e
          curl -L --fail --retry 3 -o VC_redist.x64.exe https://aka.ms/vs/17/release/vc_redist.x64.exe

          rm -rf /tmp/vcredist_stage1 /tmp/vcredist
          mkdir -p /tmp/vcredist_stage1 /tmp/vcredist app/Madeira/x86_64-vcruntime

          7zz x VC_redist.x64.exe -o/tmp/vcredist_stage1 -y
          find /tmp/vcredist_stage1 -type f -name "*.cab" -exec 7zz x "{}" -o/tmp/vcredist -y \;

          required=(
            concrt140.dll msvcp140.dll msvcp140_1.dll msvcp140_2.dll
            msvcp140_atomic_wait.dll msvcp140_codecvt_ids.dll vcamp140.dll
            vccorlib140.dll vcomp140.dll vcruntime140.dll vcruntime140_1.dll
            vcruntime140_threads.dll
          )

          for dll in "${required[@]}"; do
            source_file=$(find /tmp/vcredist -type f -iname "*$dll*" -print -quit)
            if [ -n "$source_file" ]; then
              cp "$source_file" "app/Madeira/x86_64-vcruntime/$dll"
            else
              exit 1
            fi
          done

      - name: Compile Xcode Project
        shell: bash
        run: |
          set -e
          mkdir -p build/DerivedData
          xcodebuild \
            -project app/Madeira.xcodeproj \
            -scheme Madeira \
            -configuration Release \
            -sdk iphoneos \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build

      - name: Package IPA
        if: success()
        shell: bash
        run: |
          set -e
          APP="build/DerivedData/Build/Products/Release-iphoneos/Madeira.app"
          rm -rf build/Payload
          mkdir -p build/Payload
          cp -R "$APP" build/Payload/Madeira.app
          cd build
          rm -f Madeira.ipa
          zip -r -q Madeira.ipa Payload

      - name: Upload IPA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Madeira-IPA
          path: build/Madeira.ipa
          if-no-files-found: error
